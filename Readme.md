I want to RUN a lite model on my 8GB RAM laptop
- used whisper.cpp (C++ version for faster inference)
- used pywhispercpp librbary to load 'tiny' model (75M parameters) for ASR


Steps to reproduce : 
`git clone `

Create virtual environment 
`python3 -m venv myenv`
Activate virtual environment
`source myenv/vin/activate`

Run the main.py file inside ASR folder

